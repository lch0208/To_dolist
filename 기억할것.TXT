**컨트롤러에는 @RestController 를 붙힌다.
**컨트롤러에는 서비스타입의 변수를 추가한다.
변수 추가하고 항상 생성자를 만든다.

컨트롤러에 만들어야 될것은 API클라이언트의 요청을 처리해줄 메서드(함수),
1)java함수 만들기
2)함수 위에 ~Mapping, URL 달기

**서비스에서는 @Service를 붙힌다.
**서비스에서는 리퍼지토리 타입의 변수를 추가한 후 생성자를 만든다

서비스에서 만들어야 될것은 save함수